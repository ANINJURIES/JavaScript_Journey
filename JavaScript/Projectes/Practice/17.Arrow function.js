// Arrow functions allow us to write shorter function syntax:

let myFunction = (a, b) => a * b;

hello = () => {
    return "Hello World!";
  }

  hello = () => "Hello World!";   // only for single statment 

  // arrow with paraeters 

  hello = (val) => "Hello " + val;