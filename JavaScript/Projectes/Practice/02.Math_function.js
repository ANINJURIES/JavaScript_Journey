


// x to the Power 
let x = 5;
let z = Math.pow(x,2); // Math.pow(Number,Power)
console.log(z);

// Operator Precedence

let a = 100 + 50 *3;
console.log(a);   // Multiplication (*) and division (/) have higher precedence than addition (+) and subtraction (-). so first they executes 
                  // for same precedence operator precedence will go left to right.